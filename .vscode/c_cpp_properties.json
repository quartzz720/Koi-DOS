{
    "//": "Include paths only, so an editor resolves what the compiler resolves through -I. Without this, opening koi.h shows a screen of errors on an include that is perfectly correct at build time - which is exactly the first impression somebody gets when they open the SDK. Everything here is workspace-relative on purpose: nothing about this machine belongs in the repository.",
    "version": 4,
    "configurations": [
        {
            "name": "Koi-DOS",
            "includePath": [
                "${workspaceFolder}",
                "${workspaceFolder}/include",
                "${workspaceFolder}/kernel",
                "${workspaceFolder}/programs",
                "${workspaceFolder}/sdk"
            ],
            "defines": [],
            "cStandard": "c11",
            "intelliSenseMode": "linux-gcc-x64"
        }
    ]
}
